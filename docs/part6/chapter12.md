---
title: 第12章 从Chatbot到Agent：AI产品形态如何演化
---

# 从Chatbot到Agent

<div class="part-intro">
本章内容提要：Chatbot -- 回答问题 / Copilot -- 辅助人在任务中工作 / Agent -- 有目标、能调用工具、能多步执行 / Workflow与Automation / Computer Use / 为什么能自动做事的AI更强也更危险 / 如何选择AI工具和模型
</div>

## Chatbot：一问一答

Chatbot是绝大多数人接触AI的第一站。你输入问题，AI回答。这个模式的核心特征是**反应式交互**：你不问，它不说。

**三个能力上限：**
1. **只能说，不能做** -- 纯Chatbot不能替你查实时数据、操作文件、执行代码
2. **断裂的工作流** -- AI的工作空间和你的工作空间是隔离的
3. **缺少环境感知** -- 它不知道你在用什么软件、光标在哪里

> 这三个上限不是缺陷，而是设计边界。正因为Chatbot"只会说不会做"，它也是最安全的AI形态。

## Copilot：嵌入式辅助

Copilot的本质突破是把AI从聊天窗口"请"到了你的工作环境中。

| 对比 | Chatbot | Copilot |
|------|---------|---------|
| AI在哪里 | 独立的聊天窗口 | 你的工作软件内部 |
| 上下文 | 你手动描述 | AI自动感知 |
| 交互方式 | 你问它答 | 它主动建议 |
| 控制权 | 你自己执行 | 你接受/拒绝建议 |

::: info 典型产品
- **GitHub Copilot** / **Cursor** -- 代码编辑器中的AI辅助
- **Microsoft Copilot for 365** -- Office套件中的AI助手
- **Notion AI** -- 笔记和项目管理中的AI辅助
:::

## Agent：自主多步执行

Agent是"你说目标，它去完成"。核心机制是**Agent Loop**：

**感知** -> **推理** -> **行动** -> **反馈** -> 循环直到任务完成

::: tip Agent Loop实例
你对Claude Code说："帮我把这个项目的所有测试跑一遍，修复失败的测试。"

1. Agent读取项目结构，发现使用pytest
2. 运行`pytest`，发现3个测试失败
3. 读取失败测试的错误信息，分析原因
4. 修改源代码修复问题
5. 重新运行测试验证修复
6. 重复直到全部通过
:::

**Agent的信任边界问题**：Agent自主执行十几步操作，其中的错误可能被后续步骤放大。2026年的Agent产品普遍采用**分级信任机制**：低风险操作自动执行，高风险操作需人工确认。

## Workflow与Automation

**Workflow**：步骤预先定义好，AI只在每一步内部负责内容处理。比Agent更可预测、更可调试。

**Automation**：在满足特定条件时自动启动Workflow，无需人工触发。

::: warning Automation的铁律
自动化的操作影响范围越大，人工审核的节点就应该越多。自动分类邮件可以完全自动化；自动代你发送回复至少应该让你预览确认。
:::

## Computer Use

AI通过"看屏幕、操作鼠标键盘"来使用计算机。不需要API接口，但安全风险最高。

当前的安全防线：隔离执行（沙箱）、敏感操作暂停、操作日志、权限约束。

## 能力-风险光谱

| 产品形态 | AI能做什么 | 最坏后果 | 人类控制度 |
|---------|-----------|---------|-----------|
| Chatbot | 生成文字 | 采纳错误建议 | 完全控制 |
| Copilot | 嵌入式建议 | 接受不合适修改 | 逐步审批 |
| Agent | 多步工具操作 | 文件被错误修改 | 关键节点审批 |
| Workflow | 预定义流程 | AI处理不准确 | 流程设计时控制 |
| Automation | 自动触发执行 | 错误操作反复执行 | 规则设计时控制 |
| Computer Use | 操作图形界面 | 影响任何GUI系统 | 沙箱+暂停 |

> 不是越强大的AI产品形态就越好。正确的逻辑是"用最合适的"，而不是"用最强的"。

## 选择AI工具的六个维度

1. **任务类型与产品形态匹配** -- 简单问答用Chatbot，多步执行用Agent
2. **模型能力匹配** -- 强推理选大模型，简单任务选轻量模型
3. **成本考量** -- 分层使用不同模型，避免"杀鸡用牛刀"
4. **速度需求** -- 交互式任务选快模型，后台任务可选慢但强的模型
5. **隐私与数据安全** -- 敏感数据考虑本地模型
6. **中文能力** -- 特定中文场景可能国产模型更优

::: details 本章核心回顾
- 六种AI产品形态：Chatbot、Copilot、Agent、Workflow、Automation、Computer Use
- **能力与风险同步增长**，选择原则是"用最合适的"
- Agent的核心是Agent Loop（感知-推理-行动循环）
- Workflow比Agent更可预测，适合重复性任务
- 选择工具从六个维度评估：任务类型、模型能力、成本、速度、隐私、中文能力
:::


---

<Quiz
  question="Chatbot銆丆opilot銆丄gent涓夎€呯殑鏍稿績鍖哄埆鏄粈涔堬紵"
  :options="['浠锋牸涓嶅悓', '鐣岄潰涓嶅悓', '鑷富鎬х▼搴︿笉鍚岋細瀵硅瘽鍨嬨€佸祵鍏ュ瀷銆佽嚜涓诲瀷', '浣跨敤鐨凙I妯″瀷涓嶅悓']"
  answer="鑷富鎬х▼搴︿笉鍚岋細瀵硅瘽鍨嬨€佸祵鍏ュ瀷銆佽嚜涓诲瀷"
  explain="Chatbot鏄綘闂畠绛旓紱Copilot宓屽叆鍦ㄤ綘鐨勫伐浣滄祦涓疄鏃惰緟鍔╋紱Agent鑳借嚜涓昏鍒掑拰鎵ц澶氭楠や换鍔°€傝兘鍔涜秺寮猴紝闇€瑕佺殑淇′换鍜岀洃绠′篃瓒婂銆?
/>

<ThinkBox
  question="鎯充竴涓綘鏃ュ父瀛︿範涓噸澶嶅仛鐨勪换鍔°€傚畠鏇撮€傚悎鐢–hatbot銆丆opilot杩樻槸Agent鏉ュ畬鎴愶紵涓轰粈涔堬紵"
>

鍒ゆ柇鏍囧噯锛?1) 涓€闂竴绛斿瀷鐨勪换鍔￠€塁hatbot锛?2) 闇€瑕佸湪浣犵殑宸ヤ綔鐜涓疄鏃惰緟鍔╃殑閫塁opilot锛?3) 鍖呭惈澶氫釜姝ラ涓斿彲浠ユ槑纭畾涔夊畬鎴愭爣鍑嗙殑鍙互鑰冭檻Agent銆傝浣忥細鐢ㄦ渶绠€鍗曞鐢ㄧ殑宸ュ叿銆?
</ThinkBox>
