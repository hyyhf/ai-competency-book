---
title: 第13章 AI如何长出手脚：工具、协议、技能与自动化
---

# AI如何长出手脚

<div class="part-intro">
本章内容提要：模型本身的能力边界 / Tool Use：让AI调用外部能力 / API与Schema / MCP：标准化的工具连接 / Skills、Plugins与Hooks / 权限与最小权限原则 / No-code自动化
</div>

## 模型本身能做什么，不能做什么

一个纯粹的语言模型，能做的事情归结为一件：**根据输入的文字，生成输出的文字**。

| 模型本体擅长的 | 需要工具才能做的 |
|-------------|---------------|
| 理解和生成文本 | 读取实时数据 |
| 翻译、摘要、问答 | 访问互联网 |
| 推理、代码生成 | 操作文件系统 |
| 风格转换、创意写作 | 执行代码 |
| 结构化输出生成 | 发送消息、调用外部服务 |

> 模型只能处理信息，不能改变现实。这不是缺陷，而是架构设计。

## Tool Use：让AI调用外部能力

Tool Use的核心：让模型在生成文字之外，还可以**请求调用**一个外部工具。

::: tip Tool Use的执行过程
1. 你问AI："今天武汉天气怎么样？"
2. 模型判断需要工具，生成结构化请求：`{"tool": "get_weather", "city": "武汉"}`
3. 运行时层调用真正的天气API
4. 结果返回模型
5. 模型用自然语言回答你

**模型负责决策，工具负责执行，两者分工明确。**
:::

同一个模型在不同产品中能做的事差别很大，原因不是模型不同，而是**配备的工具不同**。

## MCP：AI工具世界的"USB-C"

MCP(Model Context Protocol)是一个开放标准，让AI应用和外部工具用统一的方式沟通。

**三个角色：**
- **MCP Host** -- 你使用的AI应用（如Claude Desktop、Cursor）
- **MCP Client** -- 嵌入Host中的通信组件
- **MCP Server** -- 包装了具体工具能力的服务

**MCP Server提供三种能力：**
1. **工具(Tools)** -- AI可以主动调用的功能（动词）
2. **资源(Resources)** -- AI可以读取的数据源（名词）
3. **提示模板(Prompts)** -- 使用指导

## Tool -> Skill -> Plugin 的层次

| 层次 | 定义 | 类比 |
|------|------|------|
| **Tool** | 单个功能（一个API调用） | 单个音符 |
| **Skill** | 多个Tool的编排 + 领域知识 | 一首乐曲 |
| **Plugin** | Tool + Skill + 认证 + UI | 完整的乐队演出 |

::: info Skills的价值
你可以把自己的专业经验"教给"AI。新闻传播学生可以写一份审稿Skill：检查五个W是否完整、数据来源是否可靠、标题是否准确。模型提供通用智能，Skill提供领域规范。
:::

## Hooks：关键节点的守护者

Hook在AI行为的关键节点设置自动触发的检查。不是AI主动发起的，而是**事件驱动的自动控制**。

- 安全检查Hook：阻止AI访问敏感目录
- 代码质量Hook：AI修改代码后自动运行格式化检查
- 费用监控Hook：Token消耗超预算时暂停执行

## 权限与最小权限原则

::: warning 最小权限原则
任何实体在执行任务时，只应该被授予完成该任务所必需的最小权限。AI只需要读你的日历？不要给它修改权限。只需要访问一个仓库？不要授权所有仓库。
:::

**OAuth**是标准化的授权协议：你允许AI在特定范围内代表你访问服务，但不需要把密码给它。

实用准则：
1. 授权前仔细阅读权限范围
2. 优先选择只读权限
3. 定期清理不再使用的授权
4. 敏感数据使用本地模型

## No-code自动化

Zapier、Make、n8n等平台让非工程背景的用户通过**可视化界面**搭建AI工作流。

::: tip 实用场景
每天自动检索最新论文 -> AI生成摘要和相关度评估 -> 高相关度论文发送到你的邮箱。整个流程不需要写一行代码。
:::

::: details 本章核心回顾
- 纯粹的语言模型只能"文字进、文字出"，**工具是跨越边界的桥梁**
- **Tool Use**：模型决策 + 工具执行的协作机制
- **MCP**是AI工具连接的标准化协议
- **Skill**把领域知识封装为可复用能力，**Plugin**是完整能力包
- **最小权限原则**：只授予完成任务所需的最少权限
- No-code平台让普通学生也能搭建AI工作流
:::


---

<Quiz
  question="'鏈€灏忔潈闄愬師鍒?鍦ˋI宸ュ叿浣跨敤涓剰鍛崇潃浠€涔堬紵"
  :options="['缁橝I鏈€灏戠殑瀵硅瘽杞暟', '鍙畨瑁呮渶灏戠殑AI宸ュ叿', '鍙巿浜圓I瀹屾垚褰撳墠浠诲姟鎵€闇€鐨勬渶灏戞潈闄?, '浣跨敤鏈€渚垮疁鐨凙I妯″瀷']"
  answer="鍙巿浜圓I瀹屾垚褰撳墠浠诲姟鎵€闇€鐨勬渶灏戞潈闄?
  explain="褰揂I闇€瑕佽鍙栨枃浠躲€佽皟鐢ˋPI鎴栨墽琛屼唬鐮佹椂锛屽彧寮€鏀惧畠瀹屾垚褰撳墠浠诲姟纭疄闇€瑕佺殑鏉冮檺銆傝繖鏄畨鍏ㄤ娇鐢ˋI宸ュ叿鐨勫熀鏈簳绾裤€?
/>
